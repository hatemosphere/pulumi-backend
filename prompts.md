❯ as next step, let's remove "legacy parts of the code" mentioning some backward compatibility. this is a poc alpha project, not deployed anywhere. let's
  not generate obsolete code meant for backwards compability with non-existing systems :D

⏺ Good point! Let me clean up the plan — no backward compat shims, no dual endpoints, no deprecated anything. This is a clean break.
